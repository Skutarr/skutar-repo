--------замена
x.replace(' ',"") --> удаление пробелов

-------расширить
array.append(x) ---> добавление х в существующий массив

-------найти MIN\MAX элемент массива
min() / max()

--------знак инкремента
+=

--------счетчик(возвращает сколько раз встречался аргумент в списке )
list.count(val)